# AI Music Genre Classifier — dependencies for Render (Python 3.11)

flask>=3.0.0
flask-cors>=4.0.0

# TensorFlow 2.19+ supports Python 3.11 on Linux x86_64
tensorflow==2.19.0
tensorflow-hub>=0.16.0

# Lightweight audio I/O
soundfile>=0.12.1
resampy>=0.4.2

# MP3 / M4A / AAC / WebM decoding
pydub>=0.25.1

numpy>=1.24.0,<2.0.0
