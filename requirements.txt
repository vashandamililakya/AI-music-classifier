# AI Music Genre Classifier — slim dependencies for Render free tier
# Estimated RAM: ~180-250 MB  (vs ~700 MB with full tensorflow)

flask>=3.0.0
flask-cors>=4.0.0

# TFLite only — ~15 MB vs tensorflow's ~500 MB
tflite-runtime>=2.14.0

# Lightweight audio I/O (replaces librosa for WAV/FLAC/OGG)
soundfile>=0.12.1
resampy>=0.4.2

# MP3 / M4A / AAC / WebM decoding (uses ffmpeg under the hood)
pydub>=0.25.1

numpy>=1.24.0,<2.0.0
